name: Deploy Frontend to Hostinger

on:
  workflow_dispatch:

jobs:
  deploy-frontend:
    name: Frontend deployment placeholder
    runs-on: ubuntu-latest

    steps:
      - name: Explain required secrets
        run: |
          echo "Frontend deployment is intentionally a placeholder."
          echo "Configure the real deployment steps with a trainer or senior developer."
          echo "Required GitHub secrets:"
          echo "- HOSTINGER_FRONTEND_HOST"
          echo "- HOSTINGER_FRONTEND_USER"
          echo "- HOSTINGER_FRONTEND_SSH_KEY"
          echo "- HOSTINGER_FRONTEND_PATH"
          echo "- VITE_API_BASE_URL"
